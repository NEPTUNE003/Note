# 一、非易失性存储器（掉电不丢）

1. **Mask ROM（Mask-programmed Read-Only Memory：掩模只读存储器）**  
   - 简介：芯片生产时由制造商直接写入数据，用户无法修改。  
   - 缺点：灵活性为零，数据有误无法修正；生产周期长，不适合小批量或需更新程序的产品。

2. **PROM（Programmable Read-Only Memory：可编程只读存储器）**  
   - 简介：用户可通过专用编程器一次性写入数据。  
   - 缺点：只能写入一次，无法擦除或修改。

3. **EPROM（Erasable Programmable Read-Only Memory：可擦除可编程只读存储器）**  
   - 简介：可通过芯片窗口的紫外线擦除数据并重编程。  
   - 缺点：擦除需紫外线照射15~30分钟，芯片必须从电路板取下；只能整片擦除、无法按字节修改。

4. **EEPROM（Electrically-Erasable Programmable Read-Only Memory：电可擦除可编程只读存储器）**  
   - 简介：支持在电路板上电擦写，最小擦写单位是字节。  
   - 缺点：早期写入需高压（现代已内置电荷泵升压，可直接由单电源供电即可工作），擦写寿命约10万次。

5. **Flash Memory（闪存）**  
   - 简介：必须按块电擦除。分为NOR（Not OR或非）和NAND（Not AND与非）两类，兼具EEPROM的电擦除特性和更高的存储密度。  
   - 特性对比：  
     - **NOR Flash**：支持字节级随机寻址（通过地址线直接访问任意字节），延迟极低，支持XIP，擦除极慢（按块擦除），写一般。  
     - **NAND Flash**：不支持直接字节寻址，只能按页读取（必须先以页为单位读到内部缓存，然后才能从缓存中按字节取出）。不支持XIP。读/写/擦除整体顺序吞吐量远高于NOR，但写入前需擦除且随机写入延迟高。

---

# 二、易失性存储器（掉电即丢，主要用于程序运行时的临时数据）

6. **SRAM（Static Random-Access Memory：静态随机存取存储器）**  
   - 简介：只要持续供电，数据就能稳定保持，无需像DRAM那样周期性刷新。  
   - 缺点：存储单元（1bit）需6个晶体管（6T），集成度低、单位成本极高，不适合大容量主存。主要用于CPU缓存等小容量高速场景。

7. **DRAM（Dynamic Random-Access Memory：动态随机存取存储器）**  
   - 简介：存储单元仅需1个晶体管 + 1个电容（1T1C），集成度极高。  
   - 缺点：电容电荷会泄漏，需周期性刷新。

---

**总结**  
非易失性：Mask ROM（只读）→ PROM（一次）→ EPROM（紫外擦）→ EEPROM（字节电擦） → Flash（块电擦，高密度）。  
易失性：SRAM（6T，高速缓存） vs DRAM（1T1C，需刷新，主存）。
